const rows=document.querySelector("#orders-body");const syncStatus=document.querySelector("#sync-status");console.error("RealityCheck bundled demo: simulated analytics initialization failure");fetch("api/orders.json").then(response=>{if(!response.ok)throw new Error(`Orders request failed: ${response.status}`);return response.json()}).then(orders=>{rows.innerHTML=orders.map(order=>`<tr><td><strong>${order.id}</strong></td><td>${order.customer}</td><td>${order.status}</td><td>${order.total}</td></tr>`).join("");syncStatus.textContent="Updated just now"}).catch(error=>{console.error(error);syncStatus.textContent="Sync failed"});
