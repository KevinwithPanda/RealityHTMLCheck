---
title: Combined Operations Note
lang: en
---

## Combined operations note

This first repository-authored source file introduces a multi-input Pandoc export. The exporter receives two Markdown files in one command and emits one standalone HTML document.

### Purpose

The scenario verifies that headings and internal navigation from separate source files survive the real conversion boundary.

Continue to the [release checklist](#release-checklist).
