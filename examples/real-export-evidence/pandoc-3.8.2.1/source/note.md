---
title: Portable HTML Export Probe
lang: en
---

## Export fidelity note

This repository-owned note contains no personal data and uses no remote assets. It exists only to preserve one real Pandoc HTML export for reproducible compatibility checks.

## Sharing checklist

- The document has a title and language.
- The table has explicit column headers.
- The code sample is inert text, not executable page behavior.

## Structured evidence

| Check | Expected result |
|---|---|
| Local resources | No missing files |
| Executable scripts | None |
| Mobile metadata | Present in the standalone export |

## Inert code example

```html
<article aria-labelledby="example-title">
  <h2 id="example-title">Example only</h2>
</article>
```

The visible markup above is documentation; it is not inserted as active HTML.
